{% extends '../base.html.twig' %}

{% block title %}Booking{% endblock %}

{% block body %}
    <h1>Booking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ remplacement.id }}</td>
            </tr>
            <tr>
                <th>BeginAt</th>
                <td>{{ remplacement.beginAt ? remplacement.beginAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndAt</th>
                <td>{{ remplacement.endAt ? remplacement.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ remplacement.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_remplacement_index') }}">back to list</a>

    <a href="{{ path('app_remplacement_edit', {'id': remplacement.id}) }}">edit</a>

    {{ include('remplacement/_delete_form.html.twig') }}
{% endblock %}
